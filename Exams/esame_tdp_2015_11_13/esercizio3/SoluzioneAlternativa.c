#include <stdlib.h>
#include <stdio.h>

#include "esercizio3.h"

int* integralVector(int   *length, TipoSCL src) {
	*lenght=0;
	AUX1_integralVector(length, scrl)
	int *ris= calloc(*length.sizeof(int));
	AUX2_integralVector(srcl,ris,0);
	return ris;
}
void AUX1_integralVector(int *length, TipoSCL srcl){
	if(scrl==NULL) {return;}
	else{
		(*length)++;
		AUX_integralVector(length, srcl->next);
		
	}
void AUX2_integralVector(int*length , int* ris, int cont){
	if(scrl==NULL) {return;}
	else{
		ris[cont]= srcl->info->ris(cont-1);
		AUX2_integralVector(scrl->next.ris.cont(+1);
		return;
		
}
}
